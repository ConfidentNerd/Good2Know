Frontend for Good2Know. React + TypeScript + Vite, tailwind for styling.

To install dependencies:
```sh
bun install
```

To run:
```sh
bun run dev
```

open http://localhost:5173

/api is proxied to the server on port 32432, so run that one too.
